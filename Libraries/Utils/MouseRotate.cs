using UnityEngine;

namespace Agava.Utils
{
    public class MouseRotate : MonoBehaviour
    {
        //private const string MouseHorizontalAxis = "Mouse X";
        //private const int LeftMouseButton = 0;

        //[SerializeField, Min(0.1f)] private float _rotationSpeed;

        //private void Update()
        //{
        //    if (Input.GetMouseButtonDown(LeftMouseButton))
        //        transform.Rotate(0, -Input.GetAxis(MouseHorizontalAxis) * _rotationSpeed * Time.deltaTime, 0, Space.World);
        //}
    }
}
